import Layout from "../elements/templates/Layout";
import AuthFrame from "../components/auth/AuthFrame";

const Auth = (props) => {
  return (
    <>
      <Layout>
        <AuthFrame />
      </Layout>
    </>
  );
};

export default Auth;
